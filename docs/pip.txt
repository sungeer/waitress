
pip cache purge  # 清除缓存

pip freeze > requirements.txt
pip install -r requirements.txt


# 卸载
pip uninstall asyncmy


python -m pip install starlette uvicorn loguru httpx2 cryptography

python -m pip install sqlalchemy PyMySQL PyJWT python-dotenv


uvicorn app:app --host 0.0.0.0 --port 8000


uvicorn app:app --host 0.0.0.0 --port 8000 --env-file /home/user/waitress-testing.env
